# Database Configuration
MONGODB_URI=mongodb://localhost:27017/shooting-star
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/shooting-star

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development

# Client Configuration
CLIENT_URL=http://localhost:3000

# Optional: MongoDB Atlas (if using cloud database)
# MONGODB_ATLAS_USERNAME=your-username
# MONGODB_ATLAS_PASSWORD=your-password
# MONGODB_ATLAS_CLUSTER=your-cluster

# Optional: Email Configuration (for future features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Redis Configuration (for caching)
# REDIS_URL=redis://localhost:6379

# Optional: Analytics (for future features)
# GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID 